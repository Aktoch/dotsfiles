[alias]
	#commit
  c = commit -m
	#push
  p = push
	pf = push -f
  #remote
  r = remote -v                     # show remotes (verbose)

[core]
	editor = vim
[user]
      # These values are set in ~/.gitconfig_local

[include]
      path = ~/.gitconfig_local